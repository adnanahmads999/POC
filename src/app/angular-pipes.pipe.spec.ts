import { AngularPipesPipe } from './angular-pipes.pipe';

describe('AngularPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new AngularPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
